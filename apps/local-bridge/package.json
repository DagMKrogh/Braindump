{
  "name": "@braindump/local-bridge",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Local event bridge — receives notes and other events from external tools (Claude skills, scripts, etc.) and forwards them to the Braindump web app via WebSocket.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
